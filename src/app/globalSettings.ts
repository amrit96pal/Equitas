const newLocal = 'http://localhost:4200/assets/mockEmployeeData.json';

//This is the global file to store all teh links so as to have all at one place for better control
export const REST_ENDPOINTS=Object.freeze({
    GET_ALL_EMPLOYEE_DATA:newLocal,
})